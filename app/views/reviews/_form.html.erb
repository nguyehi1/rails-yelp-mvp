<h3>Create a new review</h3>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.label :rating %>: <%= f.select :rating, [0, 1, 2, 3, 4, 5]  %>
  <br><br>
  <%= f.submit "Add a review", class: "btn btn-primary" %>
<% end %>
